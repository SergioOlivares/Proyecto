<%= form_for(mesa) do |f| %>
  <% if mesa.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(mesa.errors.count, "error") %> prohibited this mesa from being saved:</h2>

      <ul>
      <% mesa.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :cantidadpersonas %>
    <%= f.number_field :cantidadpersonas %>
  </div>

  <div class="field">
    <%= f.label :ubicacion %>
    <%= f.text_area :ubicacion%>
  </div>

  <div class="actions">
    <%= f.submit "Crear" %>
  </div>
<% end %>