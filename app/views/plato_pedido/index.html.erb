<p id="notice"><%= notice %></p>

<h1>Insumos</h1>

<table class="table table-hover">
  <thead>
    <tr>
      <th>Plato</th>
      <th>Pedido</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @plato_pedidos.each do |mesa| %>
      <tr>
        <td><%= plato_pedido.plato %></td>
        <td><%= plato_pedido.pedido %></td>
        <td>
          <%= link_to 'Mostrar', plato_pedido, :class => "button" %></td>
        <td> <%= link_to 'Editar', edit_plato_pedido_path(plato_pedido), :class => "button"%></td>
        <td><%= link_to 'Destruir', plato_pedido, method: :delete, data: { confirm: 'Estas seguro?' },:class => "button" %></td>
      </tr>
    <% end %>
  </tbody>
</table>
<br>
<div>
<%= link_to 'Nuevo Plato Pedido', new_plato_pedido_path,:class => "button" %>
</div>